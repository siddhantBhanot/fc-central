# Build Master and Other Branches

## Build Pipeline

There will be a build pipeline for every service:

- UI services
- Backend services

Example for backend service build:

`home-loan-home-loan-orchestrator-build`

## Service Name

Service name can vary.

Examples:

- `service name-orchestrator-build`
- `servicename-ui-build`
- `servicename-build`
